// #include <algorithm>

// namespace echo_reverse_server {

// template <template ContainerType, template Predicate>
// void forEach(ContainerType&& container, Predicate&& f)
// {
//     std::for_each(
//         std::begin(container), std::end()[&](const auto& event) { HandleEvent(event); });
// }

// } // namespace echo_reverse_server
